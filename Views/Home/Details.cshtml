@model Person

@{
    ViewData["Title"] = "Person Details";
}

<h2>Person Details</h2>

<table class="table">
    <tr>
        <td>Name:</td>
        <td>@Model.Name</td>
    </tr>
    <tr>
        <td>Surname:</td>
        <td>@Model.Surname</td>
    </tr>
    <tr>
        <td>Address:</td>
        <td>@Model.City</td>
    </tr>
    <!-- Add more details as needed -->
</table>

